"use server";
import { EntryType, FinancialStatus, PaymentMethod } from "@prisma/client";import { auth } from "@/auth";import { db } from "@/lib/db";import { revalidatePath } from "next/cache";import { z } from "zod";
const schema=z.object({description:z.string().trim().min(3),type:z.nativeEnum(EntryType),category:z.string().trim().min(2),amount:z.coerce.number().positive(),competenceDate:z.coerce.date(),dueDate:z.union([z.literal(""),z.coerce.date()]).optional(),paidAt:z.union([z.literal(""),z.coerce.date()]).optional(),paymentMethod:z.union([z.literal(""),z.nativeEnum(PaymentMethod)]).optional(),status:z.nativeEnum(FinancialStatus),notes:z.string().optional()});
async function actor(){const session=await auth();if(session?.user.role!=="ADMIN")throw new Error("Apenas administradores podem alterar o financeiro.");return session.user}
const values=(value:z.infer<typeof schema>)=>({...value,dueDate:value.dueDate||null,paidAt:value.status===FinancialStatus.PAID?(value.paidAt||new Date()):null,paymentMethod:value.paymentMethod||null,notes:value.notes?.trim()||null});
export async function createFinancialEntry(data:FormData){const user=await actor();const value=values(schema.parse(Object.fromEntries(data)));const entry=await db.financialEntry.create({data:value});await db.auditLog.create({data:{userId:user.id,entity:"FinancialEntry",entityId:entry.id,action:"CREATE"}});revalidatePath("/financeiro");revalidatePath("/")}
export async function updateFinancialStatus(data:FormData){const user=await actor();const id=z.string().parse(data.get("id"));const status=z.nativeEnum(FinancialStatus).parse(data.get("status"));await db.financialEntry.update({where:{id},data:{status,paidAt:status===FinancialStatus.PAID?new Date():undefined}});await db.auditLog.create({data:{userId:user.id,entity:"FinancialEntry",entityId:id,action:"STATUS_UPDATED",changes:{status}}});revalidatePath("/financeiro");revalidatePath("/")}
