import {db} from "@/lib/db";import {formatCurrency,formatDate} from "@/lib/domain";export default async function Finance(){const items=await db.financialEntry.findMany({orderBy:{competenceDate:"desc"},take:100});return <><h1>Financeiro</h1><div className="card table-wrap"><table className="table"><tbody>{items.map(e=><tr key={e.id}><td>{formatDate(e.competenceDate)}</td><td>{e.description}</td><td>{formatCurrency(e.amount.toString())}</td><td>{e.status}</td></tr>)}</tbody></table></div></>}
