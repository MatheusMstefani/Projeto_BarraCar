import {db} from "@/lib/db";import {formatCurrency,formatDate} from "@/lib/domain";import {createFinancialEntry,updateFinancialStatus} from "./actions";
export default async function Finance(){const items=await db.financialEntry.findMany({include:{workOrder:true},orderBy:{competenceDate:"desc"},take:100});const income=items.filter(e=>e.type==="INCOME"&&e.status==="PAID").reduce((sum,e)=>sum+Number(e.amount),0);const expense=items.filter(e=>e.type==="EXPENSE"&&e.status==="PAID").reduce((sum,e)=>sum+Number(e.amount),0);return <><h1>Financeiro</h1><div className="grid"><div className="card metric"><span>Entradas pagas</span><strong>{formatCurrency(income)}</strong></div><div className="card metric"><span>Saídas pagas</span><strong>{formatCurrency(expense)}</strong></div><div className="card metric"><span>Saldo</span><strong>{formatCurrency(income-expense)}</strong></div></div><form action={createFinancialEntry} className="card form"><h2>Novo lançamento</h2><div className="grid"><label>Descrição<input name="description" required/></label><label>Tipo<select name="type"><option value="EXPENSE">Saída</option><option value="INCOME">Entrada</option></select></label><label>Categoria<input name="category" placeholder="Fornecedor, produto, operacional..." required/></label><label>Valor<input name="amount" type="number" min="0.01" step="0.01" required/></label><label>Competência<input name="competenceDate" type="date" defaultValue={new Date().toISOString().slice(0,10)} required/></label><label>Vencimento<input name="dueDate" type="date"/></label><label>Status<select name="status"><option value="PENDING">Pendente</option><option value="PAID">Pago</option><option value="OVERDUE">Vencido</option><option value="CANCELED">Cancelado</option></select></label><label>Forma de pagamento<select name="paymentMethod"><option value="">Não informada</option><option value="CASH">Dinheiro</option><option value="PIX">PIX</option><option value="CREDIT_CARD">Crédito</option><option value="DEBIT_CARD">Débito</option><option value="BOLETO">Boleto</option><option value="OTHER">Outro</option></select></label></div><label>Observação<textarea name="notes"/></label><button>Salvar lançamento</button></form><h2>Lançamentos</h2><div className="card table-wrap"><table className="table"><thead><tr><th>Data</th><th>Descrição</th><th>Tipo</th><th>Valor</th><th>Origem</th><th>Status</th></tr></thead><tbody>{items.map(e=><tr key={e.id}><td>{formatDate(e.competenceDate)}</td><td>{e.description}<br/><span className="muted">{e.category}</span></td><td>{e.type==="INCOME"?"Entrada":"Saída"}</td><td>{formatCurrency(e.amount.toString())}</td><td>{e.workOrder?`OS #${e.workOrder.number}`:"Manual"}</td><td><form action={updateFinancialStatus}><input type="hidden" name="id" value={e.id}/><select name="status" defaultValue={e.status}><option value="PENDING">Pendente</option><option value="PAID">Pago</option><option value="OVERDUE">Vencido</option><option value="CANCELED">Cancelado</option></select><button>Atualizar</button></form></td></tr>)}</tbody></table></div>{!items.length&&<div className="card empty">Nenhum lançamento registrado.</div>}</>}
