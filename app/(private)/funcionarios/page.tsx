import {db} from "@/lib/db";export default async function Employees(){const items=await db.employee.findMany({orderBy:{name:"asc"}});return <><h1>Funcionários</h1><div className="card table-wrap"><table className="table"><tbody>{items.map(e=><tr key={e.id}><td><strong>{e.name}</strong><br/><span className="muted">{e.position}</span></td><td>{e.phone}</td></tr>)}</tbody></table></div></>}
