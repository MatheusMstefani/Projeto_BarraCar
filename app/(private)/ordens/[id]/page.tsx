import Link from "next/link";
import { notFound } from "next/navigation";
import { db } from "@/lib/db";
import { formatCurrency, formatDate } from "@/lib/domain";
import { ensureWorkOrderChecklist } from "@/server/services/checklists";
import { initializeChecklist, updateChecklist, generatePdf } from "./actions";
import { PhotoUpload } from "@/components/photo-upload";
import { SignaturePad } from "@/components/signature-pad";

const tabs = [["summary","Resumo"],["services","Serviços"],["checklist","Checklist"],["photos","Vistoria e fotos"],["signatures","Assinaturas"],["documents","Documento PDF"],["history","Histórico"]] as const;
export default async function WorkOrderDetails({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{section?:string}>}){const {id}=await params;const section=(await searchParams).section??"summary";let order=await db.workOrder.findUnique({where:{id},include:{customer:true,vehicle:true,items:{include:{service:true,employee:true}},checklistItems:{include:{templateItem:true,completedBy:true},orderBy:{templateItem:{displayOrder:"asc"}}},photos:{where:{deletedAt:null},include:{uploadedBy:true},orderBy:{createdAt:"desc"}},signatures:true,documents:{orderBy:{version:"desc"}},}});if(!order)notFound();if(section==="checklist"&&!order.checklistItems.length){try{await ensureWorkOrderChecklist(id);order=await db.workOrder.findUniqueOrThrow({where:{id},include:{customer:true,vehicle:true,items:{include:{service:true,employee:true}},checklistItems:{include:{templateItem:true,completedBy:true},orderBy:{templateItem:{displayOrder:"asc"}}},photos:{where:{deletedAt:null},include:{uploadedBy:true},orderBy:{createdAt:"desc"}},signatures:true,documents:{orderBy:{version:"desc"}}}})}catch{}}const completed=order.checklistItems.filter(i=>i.status!=="NOT_CHECKED").length;const history=section==="history"?await db.auditLog.findMany({where:{entity:"WorkOrder",entityId:id},include:{user:true},orderBy:{createdAt:"desc"}}):[];return <><div className="top"><div><h1>OS #{order.number}</h1><p className="muted">{order.customer.name} · {order.vehicle.plate}</p></div><Link href="/ordens" className="button">Voltar</Link></div><nav className="tabs">{tabs.map(([key,label])=><Link className={section===key?"active":""} href={`/ordens/${id}?section=${key}`} key={key}>{label}</Link>)}</nav>{section==="summary"&&<div className="grid"><div className="card"><h2>Cliente</h2><p>{order.customer.name}<br/>{order.customer.phone}<br/>{order.customer.city}</p></div><div className="card"><h2>Veículo</h2><p>{order.vehicle.brand} {order.vehicle.model}<br/><strong>{order.vehicle.plate}</strong><br/>{order.vehicle.color}</p></div><div className="card"><h2>Situação</h2><p>{order.status}<br/>{order.paymentStatus}<br/>{formatCurrency(order.total.toString())}</p></div></div>}{section==="services"&&<div className="card">{order.items.map(i=><p key={i.id}><strong>{i.service.name}</strong> · {i.employee?.name??"A definir"} · {formatCurrency(i.unitPrice.toString())}</p>)}</div>}{section==="checklist"&&<><div className="card progress"><strong>{completed} de {order.checklistItems.length} itens verificados</strong><progress value={completed} max={order.checklistItems.length||1}/></div>{!order.checklistItems.length&&<form action={initializeChecklist} className="card"><input type="hidden" name="workOrderId" value={id}/><p>Nenhum template ativo encontrado ou checklist ainda não inicializado.</p><button>Carregar checklist</button></form>}<div className="checklist">{order.checklistItems.map(item=><form action={updateChecklist} className="card form" key={item.id}><input type="hidden" name="workOrderId" value={id}/><input type="hidden" name="answerId" value={item.id}/><strong>{item.templateItem.title}</strong><span className="muted">{item.templateItem.category}</span><select name="status" defaultValue={item.status}><option value="NOT_CHECKED">Não verificado</option><option value="NORMAL">Normal</option><option value="DAMAGED">Possui avaria</option><option value="NOT_APPLICABLE">Não se aplica</option></select>{item.templateItem.allowsNotes&&<textarea name="notes" defaultValue={item.notes??""} placeholder="Observação"/>}<button>Salvar item</button></form>)}</div></>}{section==="photos"&&<><PhotoUpload workOrderId={id}/><div className="gallery">{order.photos.map(photo=><article className="photo-card" key={photo.id}><Link href={`/api/media/photos/${photo.id}`} target="_blank"><img src={`/api/media/photos/${photo.id}`} alt={photo.description??photo.originalName}/></Link><strong>{photo.category} · {photo.region}</strong><p>{photo.description||"Sem descrição"}</p><small>{formatDate(photo.createdAt)} · {photo.uploadedBy.name}</small><a className="button" href={`/api/media/photos/${photo.id}?download=1`}>Baixar</a></article>)}</div>{!order.photos.length&&<div className="card empty">Nenhuma foto adicionada.</div>}</>}{section==="signatures"&&<><SignaturePad workOrderId={id}/><div className="gallery">{order.signatures.map(signature=><article className="photo-card" key={signature.id}><img src={`/api/media/signatures/${signature.id}`} alt={`Assinatura de ${signature.signerName}`}/><strong>{signature.signerName}</strong><small>{signature.type} · {formatDate(signature.createdAt)}</small></article>)}</div></>}{section==="documents"&&<><form action={generatePdf} className="card form"><input type="hidden" name="workOrderId" value={id}/><h2>Documento da Ordem</h2><p>Cada geração cria uma versão imutável. Baixar não cria outra versão.</p><button>{order.documents.length?"Gerar nova versão":"Gerar PDF"}</button></form><div className="card">{order.documents.map(document=><p key={document.id}><strong>Versão {document.version}</strong> · {document.status} · {formatDate(document.createdAt)} · <a href={`/api/media/documents/${document.id}`} target="_blank">Visualizar PDF</a> · <a href={`/api/media/documents/${document.id}?download=1`}>Baixar PDF</a></p>)}</div></>}{section==="history"&&<div className="timeline">{history.map(event=><article className="card" key={event.id}><strong>{event.action}</strong><p>{event.user?.name??"Sistema"} · {formatDate(event.createdAt)}</p></article>)}</div>}</>}
