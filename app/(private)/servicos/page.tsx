import {db} from "@/lib/db";import {formatCurrency} from "@/lib/domain";export default async function Services(){const items=await db.service.findMany({orderBy:[{category:"asc"},{name:"asc"}]});return <><h1>Serviços</h1><div className="card table-wrap"><table className="table"><thead><tr><th>Serviço</th><th>Categoria</th><th>Valor padrão</th><th>Duração</th></tr></thead><tbody>{items.map(s=><tr key={s.id}><td>{s.name}</td><td>{s.category}</td><td>{formatCurrency(s.defaultPrice.toString())}</td><td>{s.durationMinutes} min</td></tr>)}</tbody></table></div></>}
