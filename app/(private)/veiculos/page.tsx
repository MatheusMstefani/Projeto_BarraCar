import {db} from "@/lib/db";import {createVehicle} from "./actions";
export default async function Vehicles(){const [items,customers]=await Promise.all([db.vehicle.findMany({include:{customer:true},orderBy:{createdAt:"desc"}}),db.customer.findMany({where:{active:true},orderBy:{name:"asc"}})]);return <><h1>Veículos</h1><div className="grid"><form action={createVehicle} className="card form"><h2>Novo veículo</h2><label>Cliente<select name="customerId">{customers.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></label><label>Marca<input name="brand" required/></label><label>Modelo<input name="model" required/></label><label>Cor<input name="color" required/></label><label>Placa<input name="plate" required/></label><button>Salvar veículo</button></form><div className="card table-wrap"><table className="table"><tbody>{items.map(v=><tr key={v.id}><td><strong>{v.plate}</strong><br/>{v.brand} {v.model}</td><td>{v.customer.name}</td></tr>)}</tbody></table></div></div></>}
